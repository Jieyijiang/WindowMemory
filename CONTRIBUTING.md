# 参与贡献

感谢你改进 Window Memory。

## 提交问题

请说明 Windows 版本、显示器数量与缩放比例、目标软件名称、复现步骤、预期结果和实际结果。日志位于程序旁边的 `Data` 目录；分享前请检查其中是否包含不希望公开的窗口标题。

## 提交代码

1. 从 `main` 创建一个简短、目的单一的分支。
2. 修改后运行 `build.ps1`。
3. 运行 `dist\WindowMemory.exe --self-test`，确认返回码为 0。
4. 提交 Pull Request，解释变更目的、行为差异和验证方法。

请不要提交 `dist`、本机配置、崩溃日志或调试文件。
