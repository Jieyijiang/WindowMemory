# 更新记录

本项目采用 `主版本.次版本.修订号` 的版本编号。

## 1.0.3 - 2026-09-07

- 默认改为 Windows 窗口事件即时触发归位，不再等待 350 ms 轮询。
- 保留 1.2 s 低频扫描作为兼容性兜底，避免个别程序漏报窗口事件。
- 集成测试新增即时事件接收和恢复延迟校验。

## 1.0.2 - 2026-09-07

- 新建窗口规则默认按程序绑定，不再受窗口标题和窗口类变化影响。
- 默认关闭或最小化到系统托盘，避免自动恢复随主窗口退出而停止。
- 系统托盘菜单增加“开机自启动”开关。
- Windows 资源管理器重启后会重新注册托盘图标。
- 再次运行程序会唤回已经隐藏的主窗口，托盘入口不可见时仍可恢复操作。
- 规则更新后会立即重新应用，并增加真实窗口移动的集成自测。

## 1.0.1 - 2026-09-05

- 为 EXE、窗口标题栏、普通任务栏和系统托盘嵌入统一的多尺寸应用图标。
- 增加“最小化或关闭时收进托盘”设置，并默认保留普通任务栏按钮。

## 1.0.0 - 2026-09-04

- 首次公开版本。
- 支持单窗口位置、尺寸、显示器和最大化状态记忆。
- 支持多窗口布局存档和自定义全局快捷键。
- 支持按程序、窗口类型和标题组合匹配。
- 支持多显示器比例适配、托盘运行和便携配置。
