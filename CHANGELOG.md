# 更新记录

本项目采用 `主版本.次版本.修订号` 的版本编号。

## 1.0.8 - 2026-09-08

- 鼠标移到重复规则按钮上立即展开，离开规则表格后自动收起，不再要求点击。
- 移除遮挡规则内容的悬浮说明框，并弱化按钮悬停描边。

## 1.0.7 - 2026-09-08

- 将重复规则的箭头与数量改成更小的悬浮胶囊按钮，同时保留 40 × 40 的点击区域。

## 1.0.6 - 2026-09-08

- 重复规则组默认收起，只显示最后一条生效规则。
- 生效规则左侧增加 `▶` / `▼` 箭头和覆盖数量，可展开查看或再次收起；展开后旧规则按新到旧排列在生效规则下方。
- 规则行改为更紧凑的高度并统一垂直居中，底部操作按钮下移。

## 1.0.5 - 2026-09-08

- 相同匹配条件的重复规则改为最后一条生效，避免一个窗口被多个位置反复归位。
- 被后续规则覆盖的旧规则保留在列表中，置灰并显示“已覆盖”；删除最后一条后，上一条自动恢复。
- 快速记忆会更新最匹配且最新的现有规则，不再为同一程序反复新增规则。

## 1.0.4 - 2026-09-07

- 将即时归位从“窗口显示后扫描”前移到“窗口创建时直接处理句柄”。
- 去掉事件后的固定等待，减少窗口先显示在旧位置再跳动的闪烁。
- 集成测试要求窗口创建事件直接完成归位，不允许仅靠兜底轮询通过。

## 1.0.3 - 2026-09-07

- 默认改为 Windows 窗口事件即时触发归位，不再等待 350 ms 轮询。
- 保留 1.2 s 低频扫描作为兼容性兜底，避免个别程序漏报窗口事件。
- 集成测试新增即时事件接收和恢复延迟校验。

## 1.0.2 - 2026-09-07

- 新建窗口规则默认按程序绑定，不再受窗口标题和窗口类变化影响。
- 默认关闭或最小化到系统托盘，避免自动恢复随主窗口退出而停止。
- 系统托盘菜单增加“开机自启动”开关。
- Windows 资源管理器重启后会重新注册托盘图标。
- 再次运行程序会唤回已经隐藏的主窗口，托盘入口不可见时仍可恢复操作。
- 规则更新后会立即重新应用，并增加真实窗口移动的集成自测。

## 1.0.1 - 2026-09-05

- 为 EXE、窗口标题栏、普通任务栏和系统托盘嵌入统一的多尺寸应用图标。
- 增加“最小化或关闭时收进托盘”设置，并默认保留普通任务栏按钮。

## 1.0.0 - 2026-09-04

- 首次公开版本。
- 支持单窗口位置、尺寸、显示器和最大化状态记忆。
- 支持多窗口布局存档和自定义全局快捷键。
- 支持按程序、窗口类型和标题组合匹配。
- 支持多显示器比例适配、托盘运行和便携配置。
